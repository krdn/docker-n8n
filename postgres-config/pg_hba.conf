# PostgreSQL Client Authentication Configuration File
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections (socket)
local   all             all                                     trust

# Localhost connections
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust

# Docker network connections (n8n-network only: 172.18.0.0/16)
# Using trust for internal Docker network (isolated from external access)
# External access is blocked by listen_addresses='localhost,postgres'
host    all             all             172.18.0.0/16           trust

# Remote access disabled for security
# To access remotely, use SSH tunnel:
# ssh -L 5432:localhost:5432 user@server-ip
# Then connect to localhost:5432
